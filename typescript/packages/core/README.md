# @infer402/core
+
Internal shared schemas, Base network constants, exact decimal conversion, and typed errors used
by the TypeScript buyer and provider packages.
+
Most users should install `@infer402/client` or
`@infer402/server` instead of importing this package directly.
